#### 三、索引方式

1. 分类： 聚集索引和非聚集索引（辅助索引） 

2. **聚集索引：** 指索引项的排序方式和表中数据记录排序方式一致的索引 

   > `举例：` 我们把一本字典看做是数据库的表，那么字典的拼音目录就是聚集索引，它按照A-Z排列。实际存储的字也是按A-Z排列的。这就是索引项的排序方式和表中数据记录排序方式一致。 

    对于Innodb 数据库引擎， 主键毫无疑问是一个聚集索引 。 如果一个主键被定义了，那么这个主键就是作为聚集索引。 

3. **非聚集索引：**

